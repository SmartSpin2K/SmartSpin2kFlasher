name: smartspin2k_flasher
description: "ESP32 firmware flasher for SmartSpin2k"
publish_to: 'none'
version: 1.5.1+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  archive: ^4.0.0
  path: ^1.9.0
  path_provider: ^2.1.0
  file_picker: ^8.0.0
  url_launcher: ^6.2.0
  shared_preferences: ^2.5.4
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ss2kv3.png
